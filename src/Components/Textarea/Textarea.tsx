import React from 'react';
import './Textarea.css'

interface TextareaProps { }

export default function Textarea(props: TextareaProps) {
  return (
    <div>
      <textarea ></textarea>
    </div>
  )
}